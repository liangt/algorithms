#include <iostream>
using namespace std;

class Solution {
public:
    bool searchMatrix(vector<vector<int> > &matrix, int target) {
        int r, c;
        r = matrix.size();
        if(!r)
            return false;
        c = matrix[0].size();
    }
};

int main()
{
    return 0;
}
