/*
#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

int main(){
    string str;
    int n;
    cin>>n;
    while(n--){
        cin>>str;
        sort(str.begin(), str.end());
        for(int i=0; i<str.size()-1; i++)
            cout<<str[i]<<" ";
        if(str.size() > 0)
            cout<<str[str.size()-1]<<endl;
        else
            cout<<endl;
    }
    return 0;
}
*/
